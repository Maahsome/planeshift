package config

const (
	// ProjectCommandName is the canonical singular Project command name.
	ProjectCommandName = "project"
	// ProjectCommandAlias is the plural alias for the Project command.
	ProjectCommandAlias = "projects"
	// ProjectLabelCommandName is the canonical singular Project Labels command name.
	ProjectLabelCommandName = "project-label"
	// ProjectLabelCommandAlias is the plural alias for the Project Labels command.
	ProjectLabelCommandAlias = "project-labels"
)
