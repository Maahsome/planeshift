# Implementation Plan: Backout Unsupported APIs

- [x] **🎟️Backout Unsupported APIs**
  - **Description:** Remove the two unsupported API slices introduced by PSFT-9 commit `11097a3` (Project Features) and PSFT-10 commit `eb7278c` (workspace-scoped Project Labels). The checked-in public inventory excludes both route families, so the executable CLI, resource clients, typed models, focused tests, local Prism contract, and manual-testing checklist must stop advertising or implementing them. Preserve the supported project lifecycle, workspace Features resource, and project-scoped `/projects/{project_id}/labels/` resource.
  - **Scope:**
    - **In scope:** The Project Features command/client/model surface under `@cmd/project`, `@projectfeatures`, and `@help`; the workspace Project Labels command/client/model surface under `@cmd/projectlabel`, `@projectlabels`, and `@help`; their root/config registrations and tests; the unsupported route blocks and schemas in `@spec/plane-api.yaml`; and the stale sections in `@MANUAL_TESTING.md`.
    - **Out of scope:** `@PUBLIC_API.txt` and its intentional excluded-route note; the retained public work prompts in `@work/README.md`, `@work/01-projects.md`, and `@work/06-work-item-labels.md`; historical completed plans and reports under `@plans/completed/` and `@tickets/planned/`; the workspace Features routes; the project-scoped `/api/v1/workspaces/{workspace_slug}/projects/{project_id}/labels/` routes; existing project boolean fields/helpers; shared Plane transport/configuration/output behavior; dependencies; and unrelated resources.
  - **Implementation Tasks:**
    - [x] **Govern:** Apply the **Owned Domains**, **Forbidden Dependencies & Protected Zones**, **Architectural & Async Invariants**, and **Coding Conventions** sections of `@CONSTITUTION.md`: remove route-specific code from its existing command/resource owners, preserve `cmd.RootCmd` registration and centralized configuration, do not add dependencies or generated clients, and leave shared synchronous transport/output behavior unchanged. Use `@ARCHITECTURE.md` ADR-002 to remove the unsupported resource commands within the established Cobra hierarchy; no new package boundary or architectural decision is needed. Use `@PUBLIC_API.txt` as the route-authorization source described by `@work/README.md`, and use `@LOCAL_BUILD.md` for verification.
      - *Read `CONSTITUTION.md`, `ARCHITECTURE.md`, `PUBLIC_API.txt`, `work/README.md`, and `LOCAL_BUILD.md`; confirmed the rollback preserves existing package boundaries, centralized configuration, supported routes, and synchronous behavior.*
    - [x] **Investigate:** Reconcile the exact rollback surface against `@plans/completed/implementation-plan-project-features.md`, `@plans/completed/implementation-plan-project-labels.md`, and the two source commits. Confirm that `@cmd/project/helpers.go` retains `optionalBoolFlag` for existing Project operations while only the Project Features client/output helpers and import are removed. Confirm that the supported project-scoped label route and the separate workspace Features route are not removed accidentally.
      - *Compared both completed plans and source commits `11097a3`/`eb7278c`; confirmed `optionalBoolFlag` belongs to surviving Project operations, while workspace `/features/` and project-scoped `/projects/{project_id}/labels/` contracts remain separate from the rollback targets.*
    - [x] **Remove Project Features:** Delete `@cmd/project/features.go`, `@cmd/project/features_get.go`, `@cmd/project/features_update.go`, and `@cmd/project/features_test.go`; remove `newFeaturesCommand()` from `@cmd/project/project.go`; and remove only `projectFeaturesClient`, `outputProjectFeatures`, and the `@projectfeatures` import from `@cmd/project/helpers.go`. Delete the four `@projectfeatures` client/model test files. Remove `@help/project_features_help.go` and remove `features` from the operation list in `@help/project_help.go` without changing the remaining Project help.
      - *Removed the Project Features commands, typed client/models/tests, focused help, registration, client/output helpers, and import; retained `optionalBoolFlag` and all existing Project operation help and code.*
    - [x] **Remove Workspace Project Labels:** Delete all files in `@cmd/projectlabel` and `@projectlabels`, including their CRUD command tests and typed client/model tests. Remove the `@cmd/projectlabel` import and registration from `@cmd/root.go`; remove the positive Project Labels hierarchy assertions and commit-specific environment setup from `@cmd/root_test.go`; remove `ProjectLabelCommandName` and `ProjectLabelCommandAlias` plus their assertions from `@config/aliases.go` and `@config/aliases_test.go`; delete `@help/project_label_help.go`; and remove the Project Labels command/example and alias text from `@help/root_help.go`.
      - *Deleted all workspace Project Labels commands, clients, models, tests, and focused help; removed root/config registrations and constants while retaining root version/lazy-factory checks and centralized Plane environment bindings.*
    - [x] **Reconcile the local API contract:** Update `@spec/plane-api.yaml` to remove the project Features path, workspace-scoped `/project-labels/` paths, their implementation-specific schemas (`ProjectFeatures`, `ProjectFeaturesUpdate`, `ProjectLabel`, `ProjectLabelCreate`, `ProjectLabelUpdate`, and `ProjectLabelPage`), and the now-unused `Project Labels` tag. Keep the unrelated workspace Features tag/routes, generic shared schemas, and project-scoped `/projects/{project_id}/labels/` contract intact so the local Prism contract matches `@PUBLIC_API.txt` rather than the broader documentation-only inventory.
      - *Removed both unsupported route families, six dedicated schemas, and the unused tag; validated YAML and confirmed workspace `/features/`, project-scoped `/projects/{project_id}/labels/`, `Features`, `Labels`, and generic schemas remain.*
    - [x] **Clean active documentation:** Remove the Project Features and Project Labels sections from `@MANUAL_TESTING.md`, leaving the supported Project checklist. Do not edit the intentional exclusion note in `@PUBLIC_API.txt`, the explicit workspace-label exclusion in `@work/README.md`, or historical completed plans/reports; those records explain why these surfaces are being backed out.
      - *Removed only the stale Project Features and Project Labels sections from `MANUAL_TESTING.md`; verified `PUBLIC_API.txt`, retained work prompts, completed plans, and planned tickets were untouched.*
    - [x] **Update regression coverage:** Extend `@cmd/project/project_test.go` to assert that `features` is not registered while all existing Project operations and flags remain available. Extend `@cmd/root_test.go` to assert that neither `project-label` nor `project-labels` is registered beneath the root, and leave the existing `version`/lazy-factory assertions intact. Keep `@config/aliases_test.go` focused on the surviving Project aliases after removing the deleted constants.
      - *Added negative registration checks for `features`, `project-label`, and `project-labels`; preserved existing Project operation/flag, version, lazy-factory, and surviving alias coverage. Focused tests pass with the ambient `PLANE_API_KEY` unset.*
    - [x] **Verify:** Run `gofmt` on changed Go files; run `CI=true go test -count=1 ./...`; run the documented non-interactive build/version workflow from `@LOCAL_BUILD.md`; and run `git diff --check`. Search active source, help, manual-test, and spec files for unsupported command/package names and route paths, allowing only the intentional inventory/history references. Confirm `go.mod` and `go.sum` are unchanged, `planeshift project` and `planeshift version` retain their existing behavior, and the supported project-scoped labels and workspace Features contracts remain present.
      - *Formatted changed Go files; full `env -u PLANE_API_KEY CI=true go test -count=1 ./...`, documented build/version workflow, YAML parsing, `git diff --check`, dependency-file checks, active-surface searches, and Project/version CLI checks all pass. Only intentional negative regression assertions mention removed command names; workspace Features and project-scoped labels remain in the contract.*
    - [x] **Deliver:** Commit the implementation with the required Jira-aware conventional message, for example `chore: [PSFT-12] back out unsupported APIs`, after the verification checklist passes.
      - *Prepared the completed checklist and summary for the Jira-aware conventional commit `chore: [PSFT-12] back out unsupported APIs` after all verification checks passed.*
