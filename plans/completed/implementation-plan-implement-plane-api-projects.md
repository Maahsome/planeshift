# Implementation Plan: Implement Plane API — Projects

- [x] **🎟️Implement Plane API — Projects**
  - *Completed the PSFT-6 Projects API slice, deterministic tests, CLI/help surface, focused contract reconciliation, verification, and dependency/security audits.*
  - **Description:** Implement the eight documented Plane Project operations for ticket PSFT-6: create a project, create a project from a template, list projects, retrieve a project, update a project, archive a project, unarchive a project, and permanently delete a project. The implementation must use the PSFT-5 shared client foundation, expose the operations below the existing Cobra hierarchy, preserve the complete documented Project contract—including feature flags, nullable fields, and dynamic JSON—and remain deterministic to test without Plane credentials. The ticket file header identifies PSFT-6-20260913-0921-2901BC, while its embedded work prompt refers to Jira PSFT-2; confirm the canonical Jira key before implementation and use that key in the branch and any commit as required by @AGENTS.md.
  - **Scope:**
    - **In scope:** Add a focused route-specific Projects resource package (for example @projects/types.go and @projects/client.go), the matching deterministic unit tests, the Project command surface in @cmd/get, Project help in @help, and only the focused contract correction/model support required in @spec/plane-api.yaml and @internal/testsupport/http.go. Reuse @plane/client.go, @plane/options.go, @plane/url.go, @plane/types.go, @config/config.go, and @objects/raw_json.go rather than introducing another transport, configuration path, pagination implementation, or output-format switch.
    - **Out of scope:** Any Project Features, Project Labels, work-item, member, cycle, module, page, upload, or other resource endpoint; deprecated /issues/ aliases; generated/OpenAPI SDK code; new third-party dependencies; live Plane calls; automatic retries or asynchronous processing; credential/configuration redesign; changes to existing version behavior; or speculative validation/flags not supported by the linked Project documentation.
  - **Implementation Tasks:**
    - [x] **Govern:** Before changing the package or command boundary, apply the governing sections of @CONSTITUTION.md: **Owned Domains** keeps Cobra registration/execution, configuration resolution, output dispatch, logging, and help in their current owners; **Forbidden Dependencies & Protected Zones** requires the existing standard-library transport and prohibits new dependencies/generated clients; **Security & Resilience Hardlines** protects credentials and the existing restricted configuration behavior; **Architectural & Async Invariants** keeps commands below cmd.RootCmd, configuration in cmd/root.go, existing dependency direction, and synchronous requests; and **Coding Conventions** keeps cmd/get orchestration, config output dispatch, objects serialization, and help responsibilities intact.
      - *Applied the Constitution’s Owned Domains, Forbidden Dependencies & Protected Zones, Security & Resilience Hardlines, Architectural & Async Invariants, and Coding Conventions to the Projects slice; preserved the pending-review resource boundary and existing package ownership.*
      - [x] Use @ARCHITECTURE.md and the boundary contract in @PLANE_CLIENT.md as context. The proposed resource-package boundary is still marked as an uncharted, pending-review decision; state that explicitly in implementation notes and obtain/update an ADR only if implementation changes that boundary. Do not silently widen the shared plane package or move Viper/configuration access into the resource package.
        - *Confirmed the Projects package will remain a route-specific boundary over the existing synchronous plane.Client; the uncharted boundary remains pending review in PLANE_CLIENT.md, with no ADR change or Viper/configuration access planned.*
      - [x] Reconcile the PSFT-6 file ID and embedded PSFT-2 reference before implementation. Do not create a branch or commit with an unconfirmed ticket key; once confirmed, follow the feat/fix/chore/hotfix prefix and conventional-commit requirements in @AGENTS.md.
        - *Confirmed PSFT-6 from the ticket filename, header, plan artifact, and repository context; created feat/PSFT-6-implement-plane-api-projects from main.*
      - [x] Re-read @LOCAL_BUILD.md, @work/README.md, @work/00-api-client-foundation.md, and the completed foundation record at @plans/completed/implementation-plan-implement-the-shared-plane-api-client-foundation.md before editing, then record any official-documentation discrepancy rather than inventing a fallback.
        - *Re-read the local build workflow, work routing, foundation prompt, and completed foundation record; the official pages confirm the planned routes, trailing slashes, scopes, and the documented resource_id/project_id naming discrepancy.*
    - [x] **Contract audit:** Treat the official Project documentation as the source of truth and use @spec/plane-api.yaml only as the local mock/contract baseline. Read and cross-check @work/01-projects.md and every linked page: the Project overview, Create a project, Create project with template, List all projects, Retrieve a project, Update a project, Archive project, Unarchive project, and Delete a project pages.
      - *Cross-checked the local prompt, Prism snapshot, and current official Project documentation; implementation follows the official routes/statuses/scopes and records the three focused snapshot discrepancies for reconciliation.*
      - [x] Capture the exact operation matrix in code comments/tests so route names such as resource_id versus project_id do not leak into incorrect paths:
        - *Cross-checked the official Project overview and all eight operation pages; recorded the exact route/method/status/scope matrix in projects/client.go, including the resource_id/project_id split and the bodyless 204 lifecycle operations.*
        - Create: POST /api/v1/workspaces/{workspace_slug}/projects/; required name and identifier plus the documented optional project settings; OAuth scope projects:write; success 201 with a JSON Project.
        - Create from template: POST /api/v1/workspaces/{workspace_slug}/projects/templates/use/; required template_id and optional name, identifier, description, network, and project_lead; OAuth scope write or projects:write as documented; success 201 with a JSON Project.
        - List: GET /api/v1/workspaces/{workspace_slug}/projects/; optional cursor, expand, fields, order_by, and per_page (default 20, maximum 100); OAuth scope projects:read; success 200 with the cursor page and Project results.
        - Retrieve: GET /api/v1/workspaces/{workspace_slug}/projects/{resource_id}/ in the official page, implemented with a project ID argument; no documented query parameters; OAuth scope projects:read; success 200 with a JSON Project.
        - Update: PATCH /api/v1/workspaces/{workspace_slug}/projects/{resource_id}/ in the official page, implemented with a project ID argument; optional documented PATCH fields; OAuth scope projects:write; success 200 with a JSON Project.
        - Archive: POST /api/v1/workspaces/{workspace_slug}/projects/{project_id}/archive/ with no request body; OAuth scope projects:write; success 204 with no response body.
        - Unarchive: DELETE /api/v1/workspaces/{workspace_slug}/projects/{project_id}/archive/ with no request body; OAuth scope projects:write; success 204 with no response body.
        - Delete: DELETE /api/v1/workspaces/{workspace_slug}/projects/{resource_id}/ in the official page, implemented with a project ID argument; OAuth scope projects:write; success 204 with no response body.
      - [x] For every operation, delegate non-2xx handling to the shared plane.APIError contract, preserve status/metadata, and verify representative JSON and non-JSON failures without adding operation-specific retries or secret-bearing error text.
        - *Projects client tests route JSON and non-JSON failures through plane.APIError, assert status preservation and credential redaction, and add no retry or operation-specific error handling.*
      - [x] Confirm that the current pages use trailing slashes on all eight paths. Use project_id as the Go argument name while retaining the exact wire path, and escape workspace/project/template IDs through plane.EscapePathSegment.
        - *All eight route helpers preserve the documented trailing slash; operation tests assert escaped workspace/project segments and the dedicated templates/use route, while the template ID remains a body field because the official path has no template-ID segment.*
    - [x] **Model:** Add typed contracts in @projects/types.go (or an equally narrow Projects package file) for Project, ProjectPage, CreateProjectRequest, CreateProjectFromTemplateRequest, UpdateProjectRequest, and list options.
      - *Implemented and focused-tested all requested Projects response/request/page/list contracts with nullable, dynamic, and unknown-field preservation.*
      - [x] Model the documented identity, timestamps, description variants, counters, membership/role values, network, visibility and feature flags, emoji/icon/cover values, archive/close settings, workspace/user/project references, estimate/default-state values, template_id, and documented integration/time-tracking flags. Use pointer fields or an equivalent presence-aware representation for nullable response properties and every optional create/PATCH property so false and zero are not lost.
        - *Added the complete typed Project response and request contracts with pointer-presence semantics for nullable and optional fields, including feature, integration, estimate, and archive settings.*
      - [x] Keep icon_prop and other documented object/dynamic values as json.RawMessage-backed data. Add custom JSON marshal/unmarshal support or a lossless unknown-field map so unmodeled response fields, explicit nulls, and future Project properties survive typed decode and output. Account for the documentation’s is_deployed type inconsistency (integer in the attribute description versus boolean in the example) with a dynamic/lossless representation instead of a brittle coercion.
        - *Kept icon_prop, default_state, and is_deployed as raw JSON, added lossless unknown-field maps and custom Project JSON methods, and tested boolean/integer/null is_deployed values plus unknown response fields.*
      - [x] Preserve the response-only naming difference between request intake_view and response inbox_view exactly as documented; do not silently rename wire fields. Keep network limited to the template-create request unless the operation page audit proves another Project operation accepts it.
        - *Requests emit intake_view while Project responses decode inbox_view; network exists only on CreateProjectFromTemplateRequest, matching the audited operation pages.*
      - [x] Represent ProjectPage using the shared cursor envelope from @plane/types.go while retaining the documented next/previous cursors, booleans, counts, total values, extra_stats, and results. Also preserve the current list example’s grouped_by, sub_grouped_by, total_count, and any other unknown page keys rather than dropping them.
        - *Embedded plane.CursorPage[Project], retained grouping/total_count metadata, and preserved unknown page/result keys through custom page JSON methods and round-trip tests.*
      - [x] Keep required request fields explicit: name and identifier for normal creation, template_id for template creation. Validate required values before sending a request, but do not add undocumented enum/range validation that would reject values the API accepts.
        - *Normal creation validates nonblank name/identifier and template creation validates template_id before the injected client is called; no undocumented enum/range checks were added.*
    - [x] **Client:** Implement @projects/client.go as a thin wrapper around an injected plane.Client. The resource client must not construct an http.Client, read Viper, resolve credentials, implement pagination transport, or switch output formats.
      - *Projects client construction is injection-only; configuration, authentication, transport, pagination mechanics, errors, and output rendering remain in their existing packages.*
      - [x] Expose one method for each of the eight operations, returning typed Project/ProjectPage data where a body is documented and plane.Response metadata plus errors to callers. For archive, unarchive, and delete, pass a nil destination/body and treat the shared 204 handling as authoritative.
        - *Added Create, CreateFromTemplate, List, Get, Update, Archive, Unarchive, and Delete methods; body operations decode typed results and lifecycle methods pass nil body/destination through the shared 204 contract.*
      - [x] Build route constants/helpers with the exact trailing slash and path segments. Escape each dynamic segment, retain /projects/templates/use/ as its own route, and never infer a route from a documentation directory slug or add an /issues/ alias.
        - *Added explicit workspace/project/archive route helpers using plane.EscapePathSegment, retained /projects/templates/use/, and added no documentation-slug inference or deprecated /issues/ alias.*
      - [x] Build list query values with plane.WithPagination or plane.PaginationQuery so cursor, per_page, fields, and expand are copied/validated by the foundation; add the Project-specific order_by value without mutating caller input. Preserve the documented “-field” descending syntax as a pass-through value and enforce the shared 1–100 per_page bound rather than clamping it.
        - *ListOptions.Query delegates pagination to plane.WithPagination, adds order_by on a copied query, preserves -created_at syntax, omits the server-default page size, and rejects per_page outside 1–100.*
      - [x] Marshal only the typed request body for create, template-create, and update. Do not send a body or Content-Type for archive/unarchive/delete, and rely on plane.Client for Accept, JSON content headers, selected authentication, context/timeout handling, rate-limit metadata, bounded errors, and response-body closure.
        - *All body operations pass only typed requests; lifecycle tests assert empty bodies and absent Content-Type while shared client tests cover headers, auth, context, metadata, bounded errors, and closure.*
      - [x] Document the OAuth scopes in method comments or the contract test matrix. Scopes are token permissions, not a new request header or a second authentication path; keep authentication exclusively in @plane/client.go.
        - *Method comments and the operation matrix document projects:read, projects:write, and the template-create write/projects:write alternatives; no scope header or alternate auth path was introduced.*
    - [x] **CLI:** Add the Projects command surface under @cmd/get and register it through @cmd/get/get.go without disturbing the existing get version command or its lazy-client behavior.
      - *Implemented and tested the full get projects command surface with lazy shared-client injection, documented flags, centralized output, and quiet 204 lifecycle behavior; existing get version registration remains intact.*
      - [x] Add a parent command equivalent to planeshift get projects with operation subcommands list, create, create-template, get, update, archive, unarchive, and delete. Use positional workspace_slug first, followed by project_id where required; apply Cobra argument validation consistent with the existing get hierarchy.
        - *Registered get projects with all eight operation subcommands, exact one/two positional-argument validation, and workspace_slug/project_id ordering.*
      - [x] Expose list flags for --cursor, --per-page, --fields, --expand, and --order-by. Use the documented server default of 20 when no explicit per-page value is supplied (or omit it and rely on that server default), and surface invalid values before a request is sent.
        - *Added all documented list flags; omitted per_page when unset so Plane applies its default of 20, rejected explicit zero/out-of-range values before factory invocation, and tested query encoding.*
      - [x] Expose normal-create flags for required --name and --identifier plus only the documented optional body fields: description, project lead, default assignee, icon_prop JSON, emoji, cover image, module/cycle/issue-view/page/intake/guest feature flags, archive_in, close_in, timezone, external source/id, issue-type enablement, and time-tracking enablement.
        - *Added required name/identifier and every documented normal-create option, including dynamic icon JSON, feature flags, archive/close settings, external identifiers, and enablement flags.*
      - [x] Expose template-create flags for required --template-id plus name, identifier, description, network, and project lead. Expose update flags for the documented optional PATCH fields, including all feature/integration/default-state/estimate fields. Use pflag Changed state when constructing pointers so explicit false and zero values are sent rather than treated as absent; parse JSON-valued icon_prop without logging its contents.
        - *Added template-create and full documented update flags; Changed-state helpers preserve false/zero/empty values, icon_prop is validated as JSON locally, and tests confirm request presence without logging contents.*
      - [x] Keep get/retrieve query flags limited to those documented by the official page; do not inherit the list pagination flags onto retrieve/update/archive/delete without a contract reason.
        - *Retrieve, update, archive, unarchive, and delete commands expose no list pagination flags; tests assert get has no cursor/per-page flags, matching the official retrieve contract.*
      - [x] Resolve the injected client factory only inside command execution. Convert typed responses to objects.RawJSON through a small resource output adapter and call c.OutputData so JSON/YAML/GRON/text/table/raw dispatch remains centralized in @config/config.go; do not implement a format switch in cmd/get.
        - *Factory resolution occurs only in RunE handlers; projects.RawOutput creates objects.RawJSON and cmd/get delegates rendering to Config.OutputData without a format switch.*
      - [x] Treat successful 204 operations as quiet success with no attempted JSON decode or fabricated resource body. Return the shared structured error on failure. Ensure help/examples contain no API keys, bearer tokens, invitation data, upload fields, or presigned URLs.
        - *Archive, unarchive, and delete return quiet success on shared 204 handling; lifecycle tests assert nil body/destination, and Project help/examples contain only safe IDs and JSON examples.*
    - [x] **Help and output:** Add Project-specific descriptions/examples in @help (for example @help/projects_help.go) and ensure command help describes positional IDs, required flags, list pagination, the archive lifecycle, 204 behavior, and dynamic icon JSON without exposing credentials. If a reusable output adapter is needed, keep it alongside the Project resource and delegate all rendering to @objects/raw_json.go and @config/config.go.
      - *Added safe Project command help covering positional IDs, required/list flags, pagination, archive lifecycle, 204 quiet behavior, and dynamic icon JSON; projects.RawOutput delegates all formats to objects.RawJSON and config.OutputData.*
    - [x] **Contract reconciliation:** Update only the Project portions of @spec/plane-api.yaml if the checked-in Prism baseline is expected to track the authoritative pages.
      - *Reconciled the focused Project Prism baseline with the official pages and recorded each discrepancy in the localized spec comments.*
      - [x] Add the official list order_by query parameter and remove the currently over-broad cursor/per_page/fields/expand parameters from the retrieve operation if the page audit confirms they are unsupported there.
        - *Added inline order_by to the Project list path and removed all inherited pagination parameters from the Project retrieve operation after confirming the official pages.*
      - [x] Change the archive POST response from the current 200/201 JSON allowance to documented 204 No Content and remove its permissive request body if the page audit confirms no body. Keep template/create at 201 JSON, update/retrieve at 200 JSON, and unarchive/delete at 204.
        - *Changed Project archive to bodyless 204 NoContent; normalized both create responses to documented 201 JSON while leaving template/create at 201, retrieve/update at 200, and unarchive/delete at 204.*
      - [x] Keep any spec edit limited to this resource’s paths/parameters/responses and record the exact official-vs-snapshot discrepancy. Do not tighten global schemas or alter unrelated resources merely to make this slice look uniform.
        - *The spec diff is limited to Project paths: omitted list order_by, over-broad retrieve pagination, and archive body/200–201 allowance are recorded inline; no global schema or unrelated resource changed.*
    - [x] **Tests:** Add deterministic tests using @internal/testsupport/http.go and local httptest servers; do not require Plane credentials or network access.
      - *Completed focused resource, model, and CLI tests using local deterministic clients/servers only; no Plane credentials or network calls are required.*
      - [x] Add @projects/client_test.go table coverage for all eight methods, asserting HTTP method, exact escaped path/trailing slash, query values, selected auth header, absent unselected auth header, JSON body shape, response decoding, response status, and returned plane metadata.
        - *Added deterministic table coverage for all eight methods, including exact escaped paths/trailing slashes, query/body/header assertions, typed page/project decoding, statuses, rate-limit metadata, and 204 handling.*
      - [x] Cover both api-key and bearer factory/client modes in the Project slice, while ensuring no resource code adds or overrides auth headers. Assert list cursor/per_page/fields/expand/order_by encoding, default handling, and 1–100 validation.
        - *Project tests cover shared API-key and bearer modes with the unselected header absent, assert all five list query values including descending order, omit the default page size, and reject invalid page bounds.*
      - [x] Cover create/template/update request presence semantics, including optional fields, dynamic icon_prop, explicit false/zero pointer values, required-field failures before transport, and nullable/dynamic response values. Verify unknown Project and page fields survive decode and RawJSON output.
        - *Tests verify omitted versus explicit optional request fields, false/zero/empty presence, dynamic icon_prop, pre-transport required validation, nullable/dynamic responses, unknown Project/page fields, and RawJSON round trips.*
      - [x] Assert archive, unarchive, and delete each send no body and correctly accept 204 without decoding. Add representative JSON API errors, non-JSON errors, malformed success JSON, and status preservation for at least one body operation and one lifecycle operation.
        - *Lifecycle tests assert empty bodies, absent Content-Type, nil destinations, and quiet 204 success; Project error tests cover typed JSON/non-JSON failures and status/redaction, while shared foundation tests cover malformed success JSON.*
      - [x] Add @projects/types_test.go for round-trip JSON of the full Project sample, nulls, dynamic object values, unknown fields, page metadata, and request omission/presence behavior.
        - *Added full Project/page round-trip tests for nulls, dynamic values, both is_deployed types, unknown fields, cursor/grouping metadata, and request omission/presence/wire names.*
      - [x] Add @cmd/get/projects_test.go for registration below RootCmd/get, exact argument/flag validation, lazy factory invocation, output dispatch through config.OutputData, quiet 204 success, and preservation of existing root version/get version command behavior. Use a fake plane.Client or injected local server rather than live credentials.
        - *Added command tests for registration, exact arguments/documented flags, pre-factory validation, lazy fake-client invocation, RawJSON/config output dispatch, explicit pointer values, safe help, and quiet lifecycle success; existing version behavior remains covered by cmd tests.*
      - [x] Extend @internal/testsupport/http.go only if a narrowly reusable assertion/emitter is missing; do not add production behavior to test support.
        - *The existing deterministic testsupport helpers covered Project request/response assertions and emitters, so no shared test-support or production behavior change was necessary.*
    - [x] **Verify:** Run gofmt on all changed Go files; run CI=true go test -count=1 ./...; run the documented non-interactive build/version workflow from @LOCAL_BUILD.md; and inspect the resulting diff with git diff --check.
      - *gofmt is clean; CI=true go test -count=1 ./..., go vet ./..., the documented metadata build, jq-formatted version command, and git diff --check all pass. mise.toml exposes only the documented build task; no separate lint command is defined.*
      - [x] Confirm no go.mod/go.sum dependency changes, no generated code, no live credentials, no secret-bearing logs/help/errors, no deprecated /issues/ routes, no changed version defaults, and no regression to restricted config-file permissions.
        - *Audits show no go.mod/go.sum or existing-command/config changes, no generated/live credential artifacts, no deprecated routes in the Projects slice, no credential-bearing help/errors, unchanged version defaults, and retained 0600 restricted config creation.*
      - [x] Confirm the final acceptance matrix has a typed contract, client method, CLI surface, and deterministic tests for each of the eight operations; pagination/dynamic/null behavior and all three 204 operations are explicitly verified.
        - *Final matrix is complete for create, template-create, list, retrieve, update, archive, unarchive, and delete: each has typed models, a Projects client method, a get projects command, and deterministic tests; pagination, dynamic/null fields, and all three 204 paths are covered.*
