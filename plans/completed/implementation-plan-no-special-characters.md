# Implementation Plan: no special characters

- [x] **🎟️no special characters**
  - **Description:** Update the names generated by the opt-in functional tests so project names comply with Plane's no-special-character constraint. Preserve the existing uniqueness guarantee based on process and timestamp entropy, and apply the rule consistently to normal project creation, project updates, template-based project creation, and the project created for work-item lifecycle coverage. Keep the change limited to test data generation; production CLI/API behavior is not part of this ticket.
  - **Scope:**
    - **In scope:** The shared generated-name helper in `@functional/runner_test.go`; project-name and project-update-name construction in `@functional/functional_test.go`; focused functional-helper tests or assertions needed to prove generated names contain only the supported alphanumeric characters; relevant test documentation/comments if the naming contract needs clarification.
    - **Out of scope:** Production project validation or API request models in `@cmd/project/create.go`, `@cmd/project/create_template.go`, or `@projects/`; work-item naming semantics unless they share the corrected helper; project identifiers; new dependencies; changes to functional-test opt-in, cleanup, authentication, or API routing behavior.
  - **Implementation Tasks:**
    - [x] **Govern:** Apply the `Coding Conventions` and `Forbidden Dependencies & Protected Zones` sections of `@CONSTITUTION.md`: keep the fix in the existing `@functional/` test package, preserve package boundaries, and use only the standard library already present. No architectural decision is required; the change does not alter command registration, configuration, API clients, or runtime behavior. Confirm the implementation plan remains consistent with ADR-001 and ADR-002 in `@ARCHITECTURE.md` and the test workflow in `@LOCAL_BUILD.md`.
      - *Read CONSTITUTION.md, ARCHITECTURE.md, and LOCAL_BUILD.md; confirmed the scoped functional-test change preserves the documented package boundaries, dependencies, ADRs, and build workflow.*
    - [x] **Investigate:** Trace every `uniqueName` call and every derived project name in `@functional/functional_test.go`, including `updatedName`, the optional `create-template` flow, the primary work-item lifecycle's parent project, and the legacy compatibility flow. Confirm which characters Plane rejects and choose a deterministic normalization rule that yields non-empty, alphanumeric names while retaining enough entropy for parallel test runs.
      - *Traced all `uniqueName` call sites and the `name + "-updated"` project derivation; Plane reports `PROJECT_NAME_CANNOT_CONTAIN_SPECIAL_CHARACTERS`, so the helper will retain ASCII letters/digits, remove separators and other special characters, fall back to `generated` for an empty prefix, and preserve timestamp/process entropy.*
    - [x] **Implement:** Update `@functional/runner_test.go` so `uniqueName` emits a project-safe alphanumeric value and sanitizes hyphenated prefixes rather than reintroducing separators. Update project-specific derived names in `@functional/functional_test.go` (notably the lifecycle update suffix) to use the same safe format. Keep the existing `uniqueIdentifier` implementation and cleanup tracking unchanged.
      - *Changed `uniqueName` to normalize prefixes to ASCII alphanumerics, added a non-empty fallback and safe naming tests, changed the project lifecycle update name to `uniqueName("project-updated")`, and left `uniqueIdentifier` plus cleanup tracking unchanged.*
    - [x] **Test:** Add focused coverage alongside the functional helpers, preferably in `@functional/runner_test.go` or a new `@functional/runner_test.go`-scoped test file, asserting generated names are non-empty, contain no non-alphanumeric characters, preserve the requested prefix after normalization where applicable, and remain distinct across calls. Include representative prefixes such as `project`, `template-project`, and `work-items-project` so the current call sites are protected.
      - *Added `TestUniqueNameProducesSafeDistinctValues` for normal, hyphenated, work-item, and empty-after-normalization prefixes; the test passed after adding an atomic sequence to cover same-clock-tick calls.*
    - [x] **Verify:** Run `gofmt` on changed Go files; run `CI=true go test -count=1 ./functional` and `CI=true go test -count=1 ./...`; inspect generated values through focused assertions or test diagnostics without requiring Plane credentials; run the documented build/version workflow from `@LOCAL_BUILD.md` if the local toolchain supports it; and run `git diff --check`. Confirm no production files, `go.mod`, or `go.sum` changed.
      - *`gofmt`, the focused name test, the full functional suite, and the full repository suite passed with inherited Plane settings removed; the documented build/version check passed with `v0.0.999`; `git diff --check` passed; and only the two functional Go files plus this checklist changed, with no `go.mod` or `go.sum` diff.*
