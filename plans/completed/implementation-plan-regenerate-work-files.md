# Implementation Plan: Regenerate Work Files

- [x] **🎟️Regenerate work files**
  - **Description:** Reconcile the Plane implementation prompts with the source-controlled public endpoint inventory in `@PUBLIC_API.txt` for Jira ticket PSFT-11. The snapshot contains 13 public route groups and 143 HTTP methods: 26 current work-item methods, 24 explicitly listed deprecated `/issues/` aliases, and the remaining project, label, state, cycle, module, intake, asset, estimate, sticky, invitation, member, and user methods. Remove the 32 resource prompt files with no endpoint in the snapshot, update retained prompts to cover every listed method exactly, and refresh the index/integration guidance so future implementation work follows the public source rather than the broader documentation index.
  - **Scope:**
    - **In scope:** Read-only comparison of `@PUBLIC_API.txt` against `@work/*.md`; updates to `@work/README.md`, the retained shared prompts, and the retained public-resource prompts; removal of unsupported resource prompts; exact method/path/parameter/trailing-slash matrices; operation counts and dependency references; and documentation-only verification of one-to-one endpoint coverage.
    - **Out of scope:** Go implementation or deletion of existing working code under `@cmd`, `@config`, `@plane`, or resource packages; changes to `@PUBLIC_API.txt` itself; historical tickets/reports and completed plans; live Plane calls; new dependencies; generated API artifacts; or a decision to make the current workspace-level Project Labels implementation public when its route is explicitly excluded from the inventory.
  - **Implementation Tasks:**
    - [x] **Govern:** Before changing prompt ownership or route scope, apply the **Owned Domains**, **Forbidden Dependencies & Protected Zones**, **Architectural & Async Invariants**, **Coding Conventions**, and **Governance** sections of `@CONSTITUTION.md`. Use ADR-001 and ADR-002 in `@ARCHITECTURE.md` to retain the shared client/configuration foundation and resource-oriented work boundaries. The prompt-to-file allocation is documentation organization, not a new runtime architecture; if reconciliation would require a new package boundary, authentication model, transport, or dependency direction, flag it as an uncharted area requiring team review instead of inventing one.
      - *Applied the Constitution ownership, dependency, invariant, convention, and governance rules plus ADR-001/ADR-002; retained the shared foundation and resource boundaries, with no uncharted runtime architecture required.*
      - Treat the current `/work-items/` routes as primary. Because the ticket explicitly names `@PUBLIC_API.txt` as the source of truth and that file also lists deprecated `/issues/` aliases, document those aliases as a bounded compatibility inventory without inventing additional aliases or making them the preferred route. This resolves the older prompt wording that prohibited aliases while preserving the Constitution’s package and dependency invariants.
    - [x] **Investigate:** Build a deterministic comparison from `@PUBLIC_API.txt` and the current prompt index in `@work/README.md`. Preserve every HTTP method, exact path, path parameter spelling, and trailing slash from the inventory. Confirm the expected total of 143 methods and map the retained prompts as follows:

      | Prompt file | Public inventory ownership | Methods after reconciliation |
      | --- | --- | ---: |
      | `@work/01-projects.md` | Projects | 9 |
      | `@work/04-work-items.md` | Current core work items and relations plus deprecated core issue aliases | 16 |
      | `@work/05-work-item-states.md` | States | 5 |
      | `@work/06-work-item-labels.md` | Project-scoped labels (`[Project labels]` in the inventory) | 5 |
      | `@work/11-work-item-links.md` | Current and deprecated work-item links | 10 |
      | `@work/12-work-item-activity.md` | Current and deprecated work-item activities | 4 |
      | `@work/13-work-item-comments.md` | Current and deprecated work-item comments | 10 |
      | `@work/14-work-item-attachments.md` | Current and deprecated work-item attachments | 10 |
      | `@work/16-cycles.md` | Cycles | 14 |
      | `@work/17-modules.md` | Modules | 12 |
      | `@work/20-intake.md` | Intake issues | 5 |
      | `@work/21-assets.md` | Assets | 9 |
      | `@work/23-estimates.md` | Estimates and estimate points | 8 |
      | `@work/44-stickies.md` | Stickies | 6 |
      | `@work/46-workspace-invitations.md` | Workspace invitations | 6 |
      | `@work/47-members.md` | Workspace and project member routes | 13 |
      | `@work/48-user.md` | Current user | 1 |

      Keep `@work/00-api-client-foundation.md` and `@work/50-integration-and-verification.md` as shared process prompts; they do not contribute endpoint methods. The 17 resource prompts above sum to all 143 methods, with no new standalone resource file required.
      - *Parsed the inventory to confirm 143 methods across 13 route groups, compared it with the 51 active numbered prompts and 290-operation index, and verified the retained 17-resource allocation sums to 143 methods with exact route ownership defined by the plan.*
    - [x] **Implement:** Update the retained resource prompts with the exact public additions and removals:
      - `@work/01-projects.md`: remove the non-inventory template-creation operation; add `GET /api/v1/workspaces/{slug}/projects-lite/` and `GET /api/v1/workspaces/{slug}/projects/{project_id}/summary/`; set the scope to nine methods.
      - `@work/04-work-items.md`: remove the undocumented advanced-search operation; cover the seven current core routes, the two current `/relations/` routes, and the seven deprecated core `/issues/` aliases, including their exact route families; set the scope to 16 methods.
      - `@work/06-work-item-labels.md`: make clear that this prompt owns the public project-scoped `GET/POST /api/v1/workspaces/{slug}/projects/{project_id}/labels/` collection and `GET/PATCH/DELETE .../{label_id}/` detail routes. Do not preserve or recreate the excluded workspace-level `/project-labels/` route.
      - `@work/11-work-item-links.md`, `@work/12-work-item-activity.md`, and `@work/13-work-item-comments.md`: retain the current `/work-items/` operations and add the corresponding deprecated `/issues/` route families exactly as listed, while marking the latter compatibility-only.
      - `@work/14-work-item-attachments.md`: replace the non-inventory three-step upload-credential/upload/complete flow with the five current `/attachments/` methods and their five deprecated `/issue-attachments/` counterparts. Keep secret/presigned-data handling guidance only if it is still relevant to a listed operation.
      - `@work/16-cycles.md`: add `GET .../cycles-lite/` and `GET .../cycles/{cycle_id}/cycle-issues/{issue_id}/` to the existing lifecycle/membership operations; set the scope to 14.
      - `@work/17-modules.md`: add `GET .../modules-lite/`; set the scope to 12.
      - `@work/21-assets.md`: add `POST /api/v1/assets/user-assets/server/` plus `PATCH` and `DELETE /api/v1/assets/user-assets/{asset_id}/server/` alongside the existing user/workspace asset routes; set the scope to nine.
      - `@work/46-workspace-invitations.md` and `@work/44-stickies.md`: represent both `PUT` and `PATCH` for the documented detail update path, not just one method; set each scope to six.
      - `@work/47-members.md`: rewrite the operation list to include all 13 inventory methods: both project member route families, their collection/detail CRUD, `project-members-lite`, and both workspace member list variants. Remove any “remove workspace member” operation if it has no matching path in `@PUBLIC_API.txt`.
      - `@work/05-work-item-states.md`, `@work/20-intake.md`, `@work/23-estimates.md`, and `@work/48-user.md`: verify their existing operation lists against the inventory without adding documentation-only endpoints.
      - *Rewrote all 17 retained resource prompts for PSFT-11 with exact method/path/parameter/trailing-slash matrices, primary-versus-compatibility route guidance, updated counts, and dependencies; a deterministic comparison found 143 matrix methods matching all 143 inventory methods with no omissions or duplicates.*
    - [x] **Implement:** Reconcile the shared prompts and index in `@work/README.md`, `@work/00-api-client-foundation.md`, and `@work/50-integration-and-verification.md`. Update the source snapshot/count language from 51 prompts and 290 documentation operations to 19 retained prompt files, 13 public route groups, and 143 methods; list the retained work files in execution order; state that `@PUBLIC_API.txt` controls route inclusion; distinguish primary `/work-items/` routes from the listed deprecated aliases; and make the final verification checklist assert exact method/path coverage, not coverage of the removed documentation catalog.
      - *Rewrote the shared foundation, retained-prompt index, and integration prompt around `PUBLIC_API.txt`, 19 active numbered prompts, 13 route groups, 143 methods, execution dependencies, primary `/work-items/` routes, and explicitly bounded deprecated aliases.*
    - [x] **Remove:** Delete the 32 resource prompts with no matching public endpoint from `@work/`: `02-project-features.md`, `03-project-labels.md`, `07-work-item-types.md`, `08-custom-properties.md`, `09-custom-property-values.md`, `10-custom-property-options.md`, `15-work-item-page-links.md`, `18-pages.md`, `19-collections.md`, `22-milestones.md`, `24-time-tracking.md`, `25-epics.md`, `26-initiatives.md`, `27-initiative-labels.md`, `28-initiative-projects.md`, `29-initiative-epics.md`, `30-releases.md`, `31-project-releases.md`, `32-release-work-items.md`, `33-release-labels.md`, `34-release-tags.md`, `35-release-comments.md`, `36-release-links.md`, `37-release-changelog.md`, `38-customers.md`, `39-customer-properties.md`, `40-customer-requests.md`, `41-teamspaces.md`, `42-teamspace-members.md`, `43-teamspace-projects.md`, `45-workspace-features.md`, and `49-idp-group-sync.md`. Preserve the shared foundation and integration prompts even though they are not endpoint groups.
      - *Removed all 32 unsupported resource prompts listed by the plan and preserved the shared foundation and integration prompts.*
    - [x] **Reconcile:** Check all retained prompt dependencies and prose for stale references to deleted files, unsupported template/advanced-search/upload/workspace-label/workspace-removal operations, the old 290-operation total, or blanket alias prohibition. Keep historical artifacts outside `@work/` unchanged; only active work prompts and their index should describe the new public scope.
      - *Validated the 19 active prompt files, found no links to removed files, removed obsolete operation/policy prose, and confirmed no PSFT-2/PSFT-10 references remain in active work prompts; historical artifacts were left unchanged.*
    - [x] **Verify:** Run an inventory/count check against `@PUBLIC_API.txt` and the rewritten prompt matrices: 143 listed methods, 19 active numbered prompts, no deleted-file links, and no public path omitted or duplicated. Review `git diff --check` and Markdown links. Because this is a documentation-only reconciliation, the Go build/test workflow in `@LOCAL_BUILD.md` is not required unless implementation files are accidentally changed; confirm the final diff contains no such code changes and preserves the user-provided `@PUBLIC_API.txt`.
      - *Verified 143 inventory methods against 143 unique prompt methods with exact equality, 19 active numbered prompts, no broken relative links, no duplicate/omitted routes, clean `git diff --check`, and no Go or `PUBLIC_API.txt` changes.*
