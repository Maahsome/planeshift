# Implementation Plan: relates_to error

- [x] **🎟️relates_to error**
  - **Description:** Fix Jira ticket PSFT-13-20260916-2128-5FB88F so the current Work Item relation-create client accepts the response actually returned by Plane for a "relates_to" request. The endpoint returns a flat JSON array of relation objects, while WorkItemRelationCreateResponse is currently modeled as a nested array ([][]WorkItemRelation). The shared decoder therefore fails before the CLI can render the successful 201 response. Preserve the existing route, request body, status handling, dynamic relation fields, unknown-field retention, and configured output formats.
  - **Scope:**
    - **In scope:** The relation-create response contract and its checked-in API schema in @workitems/types.go and @spec/plane-api.yaml; relation-create client comments/typed decoding in @workitems/client.go; deterministic model and client regression coverage in @workitems/types_test.go and @workitems/client_test.go; a focused CLI output regression in @cmd/workitem/work_item_test.go using the existing fake client; and verification against the existing acceptance path in @functional/functional_test.go.
    - **Out of scope:** Relation-create request validation and flags in @cmd/workitem/relations_create.go unless a regression test exposes a separate issue; relation-list paging/contracts; Work Item routes, legacy /issues/ aliases, authentication, shared transport/error handling in @plane, output-format implementation in @objects, new dependencies, generated SDK code, live-resource cleanup changes, and edits to the historical completed Work Items plan.
  - **Implementation Tasks:**
    - [x] **Govern:** Before changing the response model or API schema, apply the “Owned Domains,” “Forbidden Dependencies & Protected Zones,” “Architectural & Async Invariants,” and “Coding Conventions” sections of @CONSTITUTION.md. Use ADR-002 in @ARCHITECTURE.md to retain the existing @workitems resource boundary, injected @plane.Client delegation, Cobra command funnel, and synchronous behavior. Treat the ticket’s Plane response and the checked-in operation contract as the evidence for a direct flat array; do not add a fallback decoder or new abstraction for an unverified nested shape. No uncharted architectural area is introduced.
      - *Read and applied the governing Constitution sections and ADR-002; confirmed the flat-array response is an in-scope contract correction with no new architectural area.*
    - [x] **Investigate:** Trace @workitems/client.go CreateRelation through @plane/client.go to confirm the destination is decoded directly into @workitems/types.go WorkItemRelationCreateResponse. Compare the ticket’s captured response, the current nested-array model/comment, @spec/plane-api.yaml WorkItemRelationCreateResponse, and @functional/functional_test.go around the existing relations-create assertion. Record that the failure occurs during JSON decoding, not route selection, request construction, authentication, or output dispatch.
      - *Confirmed CreateRelation passes its result directly to plane.Client.Do, whose JSON unmarshal rejects the captured flat array against the current [][]WorkItemRelation model; route, request, auth, status handling, and output dispatch are otherwise unchanged.*
    - [x] **Implement:** Change @workitems/types.go WorkItemRelationCreateResponse to represent a flat []WorkItemRelation response and update its contract comment. Keep WorkItemRelation’s custom lossless JSON handling so documented fields, nullable/dynamic values, and unknown relation keys remain available. Update the CreateRelation comment in @workitems/client.go to state that Plane returns a flat relation array with HTTP 201; keep the method signature, endpoint path, request validation, shared client call, and response metadata unchanged.
      - *Changed WorkItemRelationCreateResponse from [][]WorkItemRelation to []WorkItemRelation and updated the CreateRelation contract comment without changing route, validation, delegation, or metadata behavior.*
    - [x] **Reconcile contract:** Update only the Work Item relation-create response schema in @spec/plane-api.yaml so WorkItemRelationCreateResponse is an array whose items directly reference WorkItemRelation, rather than an array of arrays. Preserve the existing operation path, operation ID, request schema, 201 response, default error response, trailing slash, and source-documentation metadata. Do not alter the relation-list page schema or add unsupported relation routes.
      - *Updated only WorkItemRelationCreateResponse to directly reference WorkItemRelation items; operation path, metadata, request/response wrappers, and relation-list schema remain unchanged.*
    - [x] **Test model:** Extend @workitems/types_test.go with the ticket’s flat JSON fixture, including representative IDs, relation_type, timestamps, and an unknown field. Assert that json.Unmarshal yields the expected relation count and values, unknown data is retained, and json.Marshal round-trips the flat array without introducing an extra nesting level. Keep existing relation-list and request-presence tests unchanged.
      - *Added a flat Plane response fixture covering IDs, relation type, timestamps, dynamic values, and an unknown field; verified lossless unmarshal/marshal and ran the focused model test successfully.*
    - [x] **Test client:** Update the relation-create fixture in @workitems/client_test.go from the stale nested sample to the flat Plane response. Strengthen the relation-create table case or add a focused case that asserts POST routing, request JSON, HTTP 201 decoding, and the returned relation ID/type, so a future nested/flat mismatch fails at the resource-client boundary rather than only in a live functional run. Retain the existing shared auth/header/path/error behavior coverage.
      - *Replaced the nested fixture with a flat response and asserted the existing table case’s POST path/body/auth plus HTTP 201, relation ID, and relation type; the focused client test passes.*
    - [x] **Test CLI output:** Extend @cmd/workitem/work_item_test.go with a focused relations-create regression using the existing fakeWorkItemClient and raw JSON output configuration. Invoke the command with relation_type relates_to and one issue, return the flat response from the fake server, and assert the command succeeds and emits a non-empty JSON array containing the relation data. Do not change @cmd/workitem/relations_create.go unless this test proves the command itself needs a change.
      - *Added a fake-client CLI regression for `relates_to` with one issue, checked the current route, and verified raw output is a non-empty flat JSON array containing the relation ID/type; no command implementation change was needed.*
    - [x] **Verify:** Run gofmt on changed Go files; run CI=true go test -count=1 ./workitems ./cmd/workitem and then CI=true go test -count=1 ./...; validate the edited YAML/route contract with the repository’s available API-testing or Prism workflow; run the documented non-interactive build/version workflow from @LOCAL_BUILD.md; and run git diff --check. If functional credentials and an explicitly disposable target are available, run the existing relations-create lifecycle in @functional/functional_test.go and confirm it returns a flat non-empty JSON array without the decode error. Confirm go.mod and go.sum remain unchanged.
      - *gofmt, git diff --check, focused Work Item/CLI tests, isolated full suite, YAML/schema/route checks, Prism Compose parsing, documented build/version output, and module-file stability all passed; read-only functional command/version checks passed, while the mutating lifecycle was skipped because no explicitly disposable target was established.*
