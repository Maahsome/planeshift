# Implementation Plan: work item labels function test

- [x] **🎟️work item labels function test**
  - **Description:** Add opt-in black-box functional coverage for the newly added project-scoped Plane label commands. The test must execute the built `planeshift` binary through the existing `@functional/runner_test.go` harness, exercise the real Cobra/configuration/client/output path, and cover the complete `label`/`labels` lifecycle: create a disposable label in a generated project, list it through the canonical and plural-alias commands, retrieve it, update it, verify the update persists, and delete it with quiet 204 handling. The coverage must retain the suite's explicit target, credential, isolation, redaction, generated-resource cleanup, and no-pre-existing-resource guarantees.
  - **Scope:**
    - **In scope:** `@functional/functional_test.go` command/help coverage, generated project/label lifecycle, and cleanup; a label-ID JSON helper in `@functional/runner_test.go`; label lifecycle guidance in `@functional/README.md`; and verification through the existing `@mise.toml` `function-test` task. The test consumes the already implemented `@cmd/label/*.go`, `@labels/client.go`, `@labels/types.go`, `@cmd/root.go`, and shared configuration/output behavior without changing production code.
    - **Out of scope:** Changes to label commands, the Plane client, label models, API specifications, authentication/configuration behavior, deterministic unit or `httptest` tests, new endpoints or flags, work-item label-assignment routes, new environment variables, new third-party dependencies, live credentials in the repository, destructive operations against pre-existing resources, or a second functional-test runner. Leave `@mise.toml` unchanged unless verification identifies a concrete existing wiring defect.
  - **Implementation Tasks:**
    - [x] **Govern:** Apply the **Owned Domains**, **Forbidden Dependencies & Protected Zones**, **Security & Resilience Hardlines**, **Architectural & Async Invariants**, and **Coding Conventions** sections of `@CONSTITUTION.md` before selecting the test boundary. Use ADR-001 and ADR-002 in `@ARCHITECTURE.md` to preserve the `cmd.RootCmd`/Cobra hierarchy, centralized configuration in `@cmd/root.go`, the lazy shared `plane.ClientFactory`, synchronous execution, and existing `cmd/label`/`labels` package boundaries. This is a test-only extension and introduces no architectural change; if implementation would require a new transport, credential path, dependency direction, or package boundary, flag it as an uncharted area requiring team review. Follow `@AGENTS.md` and retain the explicit non-production target policy documented in `@functional/README.md`.
      - *Reviewed the governing Constitution sections, ADR-001/ADR-002, AGENTS.md, and functional target safeguards; confirmed the work remains a test-only executable-boundary extension with no uncharted architectural area.*
    - [x] **Investigate:** Reconcile the ticket with `@work/06-work-item-labels.md`, the completed implementation record in `@plans/completed/implementation-plan-work-item-labels.md`, `@cmd/label/label.go`, `@cmd/label/create.go`, `@cmd/label/list.go`, `@cmd/label/get.go`, `@cmd/label/update.go`, `@cmd/label/delete.go`, `@labels/client.go`, and `@labels/types.go`. Confirm that functional coverage targets exactly the five existing project-label operations: `list`, `create`, `get`, `update`, and `delete`; uses `--workspace` and `--project-id` for project-scoped routing; uses no positional argument for collection commands; and passes exactly one `label_id` positional argument to detail commands. Confirm the documented list controls (`--cursor`, `--per-page`, `--fields`, `--expand`, and `--order-by`), create/update fields, 200/201 JSON behavior, and quiet 204 delete behavior before writing the lifecycle assertions.
      - *Reconciled the work document, completed implementation plan, label commands, client, models, and executable help; confirmed the five project-label routes, exact argument contracts, context flags, list controls, create/update fields, JSON statuses, and quiet delete behavior.*
    - [x] **Implement command discovery coverage:** Extend `commandCoverage` in `@functional/functional_test.go` with the singular `label` parent, plural `labels` alias, all five canonical operations, and the `labels list` alias path. Assert the expected usage/positional argument order, `--workspace`/`--project-id` context flags, list controls, create's required `--name`, and the documented create/update field flags through executable help. Add `label` and `labels` to the root-help safety paths so every new help surface is checked by `assertNoCredentialMaterial`; keep the matrix at the executable boundary rather than importing command internals.
      - *Added black-box help coverage for label/labels parents, all five operations, the plural list alias, exact positional usage, context flags, list controls, create/update fields, and root-help credential-safety paths; the focused discovery test passes.*
    - [x] **Add label identity and cleanup support:** Add a `labelID` convenience helper beside `projectID`, `stateID`, and `workItemID` in `@functional/runner_test.go`, using the existing JSON object/string parsing and failure reporting. Add a `managedLabel` record and `registerLabelCleanup` beside the existing generated-resource cleanup helpers in `@functional/functional_test.go`. Register cleanup immediately after a successful create, delete only the generated label ID, tolerate/log already-deleted or failed cleanup through the existing redacted `Runner` output, and mark an explicitly deleted label to avoid duplicate cleanup. Register label cleanup after project cleanup so Go's LIFO `t.Cleanup` order removes the label before its generated project.
      - *Added the shared JSON label-ID helper and managed-label cleanup with generated-ID-only deletion, redacted failure logging, duplicate-delete suppression, and LIFO-compatible registration; the focused package test compiles and skips without lifecycle opt-in.*
    - [x] **Test the label lifecycle:** Add `TestLabelLifecycle` in `@functional/functional_test.go`, gated by `lifecycleConfig` and using `newRunner`. Create a uniquely named/identified disposable project, register its generated project cleanup, then create a uniquely named label with safe documented values such as a generated name and color (and only stable optional fields accepted by the target), parse and retain its ID, and register label cleanup immediately. Run canonical `label list` with a documented pagination/query option and plural-alias `labels list`, asserting valid JSON and the generated label ID/name in both responses. Run `label get` and assert the generated label is returned; run `label update` with a unique replacement name (and, where stable, a documented optional field), assert the returned ID/name, then run `label get` again to verify persistence. Finish with `label delete` plus `RequireQuietSuccess`, set the managed label as deleted, and ensure every destructive command uses only generated IDs. Use `RunJSON` so object responses are valid JSON and credential checks remain active; rely on cleanup if an intermediate assertion fails.
      - *Added the opt-in subprocess lifecycle for generated project and label resources, canonical/plural listing, retrieval, update persistence verification, quiet deletion, immediate reverse-dependency cleanup registration, and generated-ID-only destructive calls; the safe non-opt-in gate passes.*
    - [x] **Preserve the functional-test contract:** Update `@functional/README.md` to include project-scoped labels in the suite's mutating-resource description, generated-ID tracking, reverse-dependency cleanup, and stateful disposable-target requirement. Document that labels are created under generated projects and are deleted before their parent project; retain the existing `PLANE_FUNCTIONAL_RUN`, explicit `PLANE_API_URL`, auth-mode/credential, timeout, temporary `XDG_CONFIG_HOME`, redaction, and production-host safeguards. Do not add label-specific credentials or settings, and confirm the existing `@mise.toml` task already builds an isolated binary and runs the entire `@functional` package once.
      - *Updated functional documentation to include project-scoped labels, stateful disposable-target requirements, generated-ID tracking, and label-before-project cleanup; retained all existing safeguards and verified the unchanged mise task builds an isolated binary and runs `./functional` once.*
    - [x] **Verify:** Run `gofmt` on changed Go test files; run `CI=true go test -count=1 ./...`; run the non-interactive build/version workflow documented in `@LOCAL_BUILD.md`; and run `git diff --check`. When an explicitly configured disposable non-production Plane/forwarding target and test credential are available, run the existing `mise function-test` task with its required opt-in settings; otherwise validate the normal opt-in/prerequisite gate without contacting Plane Cloud. Confirm command discovery remains credential-free, label object commands return valid JSON, delete returns no output, cleanup is reverse-dependency ordered, failures are redacted, and `@go.mod`, `@go.sum`, production label code, API routes, and existing deterministic tests remain unchanged.
      - *Formatted the changed Go tests; full `CI=true go test -count=1 ./...` passed; the documented build/version workflow emitted valid metadata JSON; `git diff --check` passed; the opt-in-off mise gate refused mutation without contacting Plane; and scope review confirmed credential-free discovery, JSON/quiet-delete assertions, redacted cleanup, reverse cleanup ordering, and unchanged production/dependency/API/test files.*
