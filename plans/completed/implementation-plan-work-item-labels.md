# Implementation Plan: work item labels

- [x] **🎟️work item labels**
  - **Description:** Implement Jira ticket PSFT-17 / Work 06, the public project-scoped Plane label catalog and its complete CRUD lifecycle. The implementation must cover exactly the five nested project label routes authorized by @PUBLIC_API.txt, reuse the shared @plane client, configuration, authentication, pagination, error, and output contracts, expose the resource through the established Cobra hierarchy, preserve nullable and dynamic JSON values, and provide deterministic local coverage without Plane credentials. The official label pages define the request fields, collection controls, OAuth scopes, response/status behavior, and documented errors; the inventory remains authoritative for route inclusion and exact trailing slashes.
  - **Scope:**
    - **In scope:** A new @labels route/model package and tests; a top-level singular label command with plural labels alias under @cmd/label; lazy registration in @cmd/root.go; command constants/tests in @config/aliases.go; safe help in @help/label_help.go and root discoverability in @help/root_help.go; focused nested-label route and schema reconciliation in @spec/plane-api.yaml; and narrowly scoped command/model/test support needed to exercise all five operations.
    - **Out of scope:** The excluded workspace-level /api/v1/workspaces/{slug}/project-labels/ route; initiative, release, customer, or other label resources; work-item label-assignment routes; any route outside the five-entry matrix; changes to @PUBLIC_API.txt; a second transport/configuration/authentication/output path; retries or asynchronous processing; generated/OpenAPI SDK code; new third-party dependencies; live Plane calls; or unrelated changes to Project, State, Work Item, version, or configuration-security behavior.
  - **Implementation Tasks:**
    - [x] **Govern:** Before choosing the resource package and CLI boundary, apply the Owned Domains, Forbidden Dependencies & Protected Zones, Security & Resilience Hardlines, Architectural & Async Invariants, and Coding Conventions sections of @CONSTITUTION.md. Use @ARCHITECTURE.md ADR-002 to keep the resource command rooted below cmd.RootCmd, use the singular label command with plural labels alias, keep route logic in @labels, retain lazy plane.ClientFactory construction, and preserve the observed cmd → supporting-package dependency direction. Use existing context resolution for workspace/project identity, keep all work synchronous, and flag any need for a new transport, dependency direction, authentication model, or package boundary as an uncharted area requiring team review.
      - *Applied the governing Constitution sections and ADR-002: labels will use the existing synchronous, lazy, resource-oriented command and client boundaries without new dependencies or uncharted architecture.*
    - [x] **Audit:** Re-read @AGENTS.md, @CONSTITUTION.md, @ARCHITECTURE.md, @LOCAL_BUILD.md, @work/README.md, @work/00-api-client-foundation.md, @work/01-projects.md, @work/06-work-item-labels.md, @PUBLIC_API.txt, @PLANE_CLIENT.md, @API_TESTING.md, @plane/client.go, @plane/types.go, @plane/url.go, @projects/client.go, @projects/types.go, @states/client.go, @states/types.go, @cmd/state, @cmd/project, @config/context.go, @config/config.go, @objects/raw_json.go, @internal/testsupport/http.go, and the completed @plans/completed/implementation-plan-work-item-states.md. Read the six official operation pages at @https://developers.plane.so/api-reference/label/overview.md, @https://developers.plane.so/api-reference/label/add-label.md, @https://developers.plane.so/api-reference/label/list-labels.md, @https://developers.plane.so/api-reference/label/get-label-detail.md, @https://developers.plane.so/api-reference/label/update-label-detail.md, and @https://developers.plane.so/api-reference/label/delete-label.md before coding. Record the following matrix beside the client so documentation naming cannot change the wire contract:
      - *Audited the repository contracts, adjacent Project/State implementations, inventory, shared test seams, completed State plan, and canonical official label pages; confirmed the five-route scope, statuses/scopes, field/query contract, and exclusion of workspace-level project labels.*
      - **List:** GET /api/v1/workspaces/{slug}/projects/{project_id}/labels/; workspace slug and project UUID; query cursor, expand, fields, order_by, and per_page (default 20, maximum 100); OAuth scope projects.labels:read; HTTP 200 cursor page with grouped_by, sub_grouped_by, total_count, shared cursor metadata, extra_stats, and label results. The page prose mentions name/color filtering, but the explicit query-parameter section names only the five controls above; do not invent undocumented name or color query keys unless the authoritative operation page is revised.
      - **Create:** POST /api/v1/workspaces/{slug}/projects/{project_id}/labels/; required name; optional color, description, external_source, external_id, parent, and sort_order; OAuth scope projects.labels:write; HTTP 201 JSON label response.
      - **Get:** GET /api/v1/workspaces/{slug}/projects/{project_id}/labels/{label_id}/; workspace slug, project UUID, and label UUID; no documented query parameters; OAuth scope projects.labels:read; HTTP 200 JSON label response.
      - **Update:** PATCH /api/v1/workspaces/{slug}/projects/{project_id}/labels/{label_id}/; optional name, color, description, external_source, external_id, parent, and sort_order; OAuth scope projects.labels:write; HTTP 200 JSON label response. Preserve omitted properties separately from explicit empty strings and numeric zero.
      - **Delete:** DELETE /api/v1/workspaces/{slug}/projects/{project_id}/labels/{label_id}/; OAuth scope projects.labels:write; HTTP 204 with no response body.
      - **Shared behavior:** Delegate selected API-key or bearer authentication, JSON headers, context cancellation, response closure, rate-limit metadata, 2xx/204 handling, bounded/redacted API errors, and malformed/non-JSON error behavior to @plane/client.go. Do not add resource-local auth headers, OAuth scope headers, transport, retries, or error parsing.
    - [x] **Reconcile the contract specification:** Update only the nested project-label paths and focused schemas in @spec/plane-api.yaml. Align both path blocks with the inventory’s /api/v1/workspaces/{slug}/projects/{project_id}/labels/ and /{label_id}/ forms, including the slug path-parameter spelling and every trailing slash, while retaining the documented operation IDs and x-plane-documentation links unless a narrowly scoped consistency correction is required. Add list-only OrderBy alongside Cursor, PerPage, Fields, and Expand; remove inherited pagination/filter controls from detail GET; make create’s request body required and reference LabelCreate with a 201 Label response; make update reference LabelUpdate with a 200 Label response; and retain 204 NoContent for delete. Add focused Label, LabelPage, LabelCreate, and LabelUpdate schemas with additional response-field tolerance, required create name, optional PATCH properties, numeric sort_order, nullable parent/association fields, cursor/grouping metadata, and nullable extra_stats. Do not add or modify the excluded workspace-level project-label route or unrelated schemas.
      - *Reconciled only the nested project-label paths and focused schemas; added list order_by, removed detail queries, wired typed create/update bodies and responses, retained delete 204, and validated the YAML/diff.*
    - [x] **Define typed contracts:** Add @labels/types.go with a Label response model for the documented id, created_at, updated_at, name, description, color, sort_order, created_by, updated_by, project, workspace, parent, external_source, and external_id values. Use typed scalar fields where stable, pointers/presence tracking for nullable or omitted values, and json.RawMessage-compatible handling for nullable, expanded, or otherwise dynamic association values; preserve future object, array, scalar, and null keys in copied Unknown data through custom marshal/unmarshal logic. Add LabelPage based on plane.CursorPage[Label], retaining grouped_by, sub_grouped_by, total_count, all shared cursor fields, explicit extra_stats null, and unknown page keys. Add CreateLabelRequest with required Name and pointer optionals, and UpdateLabelRequest with pointer fields for every optional PATCH property; nil must omit a key while explicit empty strings and sort_order 0 remain present. Validate only the documented required create name and do not invent color-format, UUID, sort-order-range, parent, or project validation.
      - *Added and formatted labels/types.go with typed scalar contracts, raw association values, presence-aware round trips, unknown-field preservation, focused page metadata, request presence semantics, and shared list-query modeling; package compilation passes.*
    - [x] **Implement the resource client:** Add @labels/client.go as a thin injected wrapper over plane.Client, following @projects/client.go and @states/client.go. Keep the exact five-operation matrix and OAuth scope comments beside the methods. Build collection/detail path helpers with plane.EscapePathSegment for workspace slug, project ID, and label ID, preserving the inventory’s trailing slashes. Expose Create, List, Get, Update, and Delete methods returning typed Label or LabelPage values plus plane.Response metadata where a body exists, and status/error metadata for delete. Send typed bodies only for Create and Update, nil bodies for Get and Delete, list-only url.Values from Label ListOptions, typed destinations for JSON responses, and no resource-local query/auth/error logic.
      - *Added labels/client.go with the exact five-route matrix, documented OAuth scopes, escaped collection/detail paths, typed request/response delegation, and shared status/error metadata; package compilation passes.*
    - [x] **Build collection options:** Add Label ListOptions and Query behavior in @labels/types.go or a narrowly focused companion. Delegate cursor, per_page, fields, and expand to plane.WithPagination, add order_by without discarding shared values, preserve comma-separated fields/expand strings and cursor encoding, and enforce the shared 1–100 page-size range while allowing zero to omit per_page and use Plane’s default. Do not expose detail-query controls or unlisted name/color query keys.
      - *Added labels.ListOptions.Query with shared pagination validation and exact order_by support; zero omits per_page and no undocumented filter/detail controls are exposed.*
    - [x] **Add CLI hierarchy and registration:** Add LabelCommandName = label and LabelCommandAlias = labels to @config/aliases.go, with focused assertions in @config/aliases_test.go. Create @cmd/label/label.go plus one operation file each for create.go, list.go, get.go, update.go, and delete.go. Register the parent from @cmd/root.go with the same lazy factory used by Project and State. The parent must construct without network/configuration credential access. Use --workspace and --project-id context override flags on every operation; list/create take no positional route arguments, while get/update/delete take exactly one label_id positional argument.
      - *Added label/labels aliases and tests, the five-operation cmd/label hierarchy, context-aware exact argument validation, and lazy root registration; command/config packages compile and pass tests.*
    - [x] **Wire request flags and output:** Add @cmd/label/helpers.go for lazy labels client construction, route-context resolution through config.ResolveRouteContext, changed-aware string/float flag extraction, and centralized output. Create must require --name and expose optional --color, --description, --external-source, --external-id, --parent, and --sort-order; update exposes the same fields without requiring any, allowing an empty update to serialize as {}. List exposes only --cursor, --per-page, --fields, --expand, and --order-by. Preserve explicit empty strings and sort_order 0 via Cobra Changed checks, validate list page size before resolving the factory, route successful responses through objects.NewRawJSONFromValue and Config.OutputData, defaulting to JSON when no output format is selected, and keep successful 204 delete quiet.
      - *Added lazy label construction, context resolution, changed-aware string/float flags, required create name, empty update support, documented list controls, centralized output defaults, pre-factory pagination validation, and quiet delete behavior; focused command compilation passes.*
    - [x] **Add safe help:** Create @help/label_help.go and update @help/root_help.go only enough to make the resource discoverable. Document the label/labels command names, workspace/project context behavior, the five operations, label_id argument ordering, list cursor/per-page/fields/expand/order_by controls, create’s required name, update’s partial semantics, color/description/external-source/external-id/parent/sort-order fields, 201/200 response behavior, 204 delete behavior, and existing JSON/YAML/GRON/text/table/raw output formats. Use placeholder identifiers only and ensure help contains no API keys, bearer tokens, invitation secrets, presigned URLs, upload metadata, or token material.
      - *Added safe project-label help and root discoverability for label/labels, context defaults/overrides, all operations/arguments/controls/fields/statuses/formats; help and command packages compile and pass focused tests.*
    - [x] **Test typed models:** Add @labels/types_test.go covering the official Label object and page examples, every documented wire name, float sort_order, nullable parent/association values, omitted versus explicit null fields, unknown scalar/object/array/null retention, grouped/cursor/count metadata, extra_stats null, and JSON round trips. Assert exact create/update request serialization, required-name validation, nil omission, explicit empty strings, explicit sort_order 0, and list query encoding/range validation. Reject malformed documented scalar types rather than weakening the contract to pass tests.
      - *Added passing deterministic label model tests for documented fields, float and raw associations, null/omission/unknown preservation, page metadata, exact request bodies, name validation, list query encoding/ranges, and malformed scalar rejection.*
    - [x] **Test all client operations:** Add table-driven local @labels/client_test.go coverage using @internal/testsupport/http.go for Create, List, Get, Update, and Delete. Assert exact HTTP method, escaped workspace/project/label path segments, trailing slashes, list query values including order_by, absence of detail queries, selected API-key or bearer header with the other credential header absent, Accept, body-only Content-Type, exact JSON request bodies, typed response/page decoding, 201/200/204 status handling, grouping/cursor/unknown fields, explicit nulls, and rate-limit metadata. Include validation-before-request, typed API errors, bounded non-JSON errors, malformed-success JSON, empty success, and 204/no-content cases without live credentials.
      - *Added passing table-driven local HTTP coverage for all five label methods, escaped routes, auth/header isolation, exact bodies/queries, typed decoding, statuses, metadata, null/unknown fields, validation, error bounding/redaction, malformed JSON, empty success, and 204 behavior.*
    - [x] **Test CLI behavior:** Add @cmd/label/label_test.go and extend @cmd/root_test.go and @config/aliases_test.go only as needed. Verify root parentage, canonical/plural alias resolution, exact operation argument counts/order, documented flags and absence of unsupported flags, lazy factory invocation, saved-context defaults and non-mutating overrides, list query construction, create/update typed bodies including empty/zero presence, configured output dispatch, safe help, and quiet delete behavior. Use a fake injected plane.Client or local httptest server; never require Plane credentials or network access.
      - *Added passing local fake-client CLI tests plus root/alias assertions for label hierarchy, aliases, exact args/flags, lazy construction, context behavior, query validation, presence-aware bodies, centralized output, safe help, and quiet delete.*
    - [x] **Audit shared boundaries and security:** Reuse @plane/client.go, @plane/types.go, @plane/url.go, @internal/testsupport/http.go, @objects/raw_json.go, @config/context.go, @config/config.go, and @config/plane.go as-is where possible. Extend shared support only for a genuinely reusable assertion or model helper; do not add label-specific production behavior to shared transport/config/output packages. Confirm API keys, bearer tokens, secret-bearing URLs, and sensitive response values cannot enter logs, errors, help, or accidental default output; keep @go.mod and @go.sum unchanged.
      - *Audited the scoped diff and secret/route scans: labels uses existing shared seams, no shared production or dependency files changed, help/client production code is secret-safe, only the five project-label routes were added, and diff checks are clean.*
    - [x] **Verify the acceptance matrix:** Confirm each of Create, List, Get, Update, and Delete has exactly one typed contract, client method, CLI command, and deterministic test, with the following route/status mapping:
      - **Create:** POST collection, required name plus documented optionals, projects.labels:write, 201 JSON Label.
      - **List:** GET collection, five documented query controls, projects.labels:read, 200 LabelPage.
      - **Get:** GET detail, no query controls, projects.labels:read, 200 Label.
      - **Update:** PATCH detail, all documented fields optional with presence-aware serialization, projects.labels:write, 200 Label.
      - **Delete:** DELETE detail, no body, projects.labels:write, 204 and no output.
      - Also verify escaped path segments, exact trailing slashes, nullable/dynamic/unknown JSON preservation, bounded structured errors, safe help/output, absence of workspace /project-labels/ and unrelated label routes, no generated/dependency changes, and no live API calls.
      - *Confirmed the complete five-operation acceptance matrix, exact route/status/scope mapping, shared-boundary behavior, presence/dynamic/unknown preservation, safe output/help, excluded-route absence, dependency stability, and deterministic local-only coverage.*
      - [x] **Verify:** Run gofmt on changed Go files; run CI=true go test -count=1 ./...; run the documented non-interactive build/version workflow from @LOCAL_BUILD.md; validate @spec/plane-api.yaml; run git diff --check; and inspect the final file list for scope. Confirm existing Project, State, Work Item, Version, output, lazy-configuration, and restricted 0600 config behavior remain intact.
        - *Formatted all changed Go files; full tests pass with the opt-in functional lifecycle disabled; the documented build/version workflow emits valid metadata JSON; the label OpenAPI contract and diff checks pass; scope, existing-resource tests, lazy configuration, output, and restricted-config behavior remain intact.*
