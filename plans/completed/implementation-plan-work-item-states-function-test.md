# Implementation Plan: work item states function test

- [x] **🎟️work item states function test**
  - **Description:** Add opt-in black-box functional coverage for the newly registered Plane Work Item States CLI. The test must execute the built `planeshift` binary through the existing `@functional/runner_test.go` harness, exercise the real Cobra/configuration/client/output path, and cover the complete project-scoped state lifecycle: create a disposable custom state, list it, retrieve it, update it, and delete it with quiet 204 handling. The coverage should also make the `state`/`states` command hierarchy discoverable and preserve the suite's credential, target, isolation, and cleanup guarantees.
  - **Scope:**
    - **In scope:** `@functional/functional_test.go` command/help coverage and the state lifecycle; a state-ID or equivalent JSON extraction helper in `@functional/runner_test.go`; generated-state cleanup guidance in `@functional/README.md`; and verification through the existing `@mise.toml` `function-test` task. The test will consume the already implemented `@cmd/state/*.go`, `@states/client.go`, `@states/types.go`, and shared `@cmd/root.go` behavior without changing production code.
    - **Out of scope:** Changes to state commands, the Plane client, state models, API specifications, deterministic unit/`httptest` tests, authentication/configuration behavior, new endpoints or flags, new environment variables, new third-party dependencies, live credentials in the repository, destructive operations against pre-existing resources, or a second functional-test runner.
  - **Implementation Tasks:**
    - [x] **Govern:** Apply the **Owned Domains**, **Forbidden Dependencies & Protected Zones**, **Architectural & Async Invariants**, **Security & Resilience Hardlines**, and **Coding Conventions** sections of `@CONSTITUTION.md` before selecting the test boundary. Use ADR-001 and ADR-002 in `@ARCHITECTURE.md` to retain the `cmd.RootCmd`/Cobra hierarchy, centralized configuration in `@cmd/root.go`, the existing lazy `plane.ClientFactory`, synchronous execution, and current package boundaries. This is a test-only extension within the existing functional package; no new architectural boundary or dependency is planned. Follow `@AGENTS.md` and keep the explicit non-production target policy from `@functional/README.md`; if implementation requires a new transport, credential path, or package boundary, record it as an uncharted area requiring team review.
      - *Applied the Constitution and ADR-001/ADR-002: the work remains an executable-boundary functional test using the existing Cobra/configuration/client path, with no new dependency, transport, credential path, package boundary, or architecture decision.*
    - [x] **Investigate:** Reconcile the ticket with `@work/05-work-item-states.md`, the completed implementation record in `@plans/completed/implementation-plan-work-item-states.md`, `@cmd/state/state.go`, `@cmd/state/create.go`, `@cmd/state/list.go`, `@cmd/state/get.go`, `@cmd/state/update.go`, `@cmd/state/delete.go`, `@states/client.go`, and `@states/types.go`. Confirm that the functional matrix targets exactly the five existing operations (`list`, `create`, `get`, `update`, and `delete`), uses `workspace_slug project_id` for collection commands and adds `state_id` for detail commands, creates a non-default custom state that is eligible for deletion, and relies on documented 200 JSON versus 204 no-content behavior.
      - *Confirmed the five existing state operations, collection/detail argument order, required create flags, list pagination controls, a deletable non-default `started` state, and 200 JSON versus 204 no-content behavior from the work ticket, implementation record, commands, client, and models.*
    - [x] **Implement command discovery coverage:** Extend `commandCoverage` in `@functional/functional_test.go` with the singular `state` parent, plural `states` alias, all five canonical operations, and at least the `states list` alias path. Assert the documented usage/positional argument order, require `--name` and `--color` on create, and ensure all help output—including `state`, `states`, and root help—passes the existing credential-material safety checks. Keep the matrix on the executable boundary rather than importing command internals.
      - *Added singular/plural state parent help, all five canonical operation paths, the `states list` alias, exact argument usage, create flag assertions, and root/help credential-safety coverage.*
    - [x] **Implement lifecycle cleanup support:** Add a `managedState` record and `registerStateCleanup` beside the existing `managedProject`/`managedWorkItem` helpers in `@functional/functional_test.go`, or add only the narrowly equivalent helper required by the lifecycle. Capture the generated state ID immediately after a successful create, register cleanup immediately, delete only that generated state ID, tolerate and log already-deleted cleanup failures using the runner's redaction, and register state cleanup after project cleanup so Go's LIFO `t.Cleanup` order removes the child state before the generated project. Add a `stateID` convenience helper beside `projectID` and `workItemID` in `@functional/runner_test.go` if it improves readability without duplicating JSON parsing.
      - *Added `managedState`/`registerStateCleanup` with generated-ID-only deletion, redacted cleanup logging, deleted-state tracking, and a JSON-based `stateID` helper; lifecycle registration will follow project registration for child-before-project LIFO cleanup.*
    - [x] **Test the state lifecycle:** Add `TestStateLifecycle` to `@functional/functional_test.go`, gated by the existing `lifecycleConfig`/`PLANE_FUNCTIONAL_RUN` contract. Use `newRunner` and a uniquely generated project name/identifier to create a test-owned project, register its cleanup, then create a uniquely named custom state through `state create` with documented required `--name`/`--color` values and a non-default, deletable configuration such as the supported `started` group. Parse and retain the returned state ID, assert valid JSON and the generated state name, run canonical `state list` with a documented pagination option, run plural-alias `states list`, and verify both include the generated state. Run `state get` and assert the generated ID/name, run `state update` with a unique replacement name, assert the updated JSON, retrieve it again to confirm persistence, and finish with `state delete` plus `RequireQuietSuccess` to verify the 204 path. Use only generated IDs for destructive calls and leave cleanup to repair the state/project if an intermediate assertion fails.
      - *Added the opt-in `TestStateLifecycle`: it creates a generated project and non-default `started` state, registers immediate child cleanup, validates canonical/plural list discovery, get/update/get persistence, and quiet generated-ID-only delete.*
    - [x] **Preserve the existing functional contract:** Update `@functional/README.md` only where its lifecycle description must include generated states, child-before-project cleanup, and the requirement for a stateful disposable Plane/forwarding proxy for create/read/update/delete. Reuse the existing `PLANE_API_URL`, auth-mode, credential-redaction, temporary `XDG_CONFIG_HOME`, timeout, and opt-in rules; do not add state-specific credentials or environment variables. Leave the already integrated `@mise.toml` `function-test` task unchanged unless verification exposes a concrete wiring defect.
      - *Updated only the README lifecycle wording for generated projects/states/work items, child-before-project cleanup, and a stateful disposable Plane or forwarding proxy; retained all existing settings and left `mise.toml` unchanged.*
    - [x] **Verify:** Run `gofmt` on changed Go test files, `CI=true go test -count=1 ./...`, the non-interactive build/version workflow documented in `@LOCAL_BUILD.md`, and `git diff --check`. Run `mise function-test` only against an explicitly configured disposable non-production target with test credentials when available; otherwise confirm the normal opt-in/prerequisite skip or gate behavior without contacting Plane Cloud. Verify that command discovery remains credential-free, lifecycle object commands return valid JSON, delete returns no output, cleanup is reverse-dependency ordered, and `go.mod`/`go.sum`, production state code, API routes, and existing deterministic tests remain unchanged.
      - *Ran gofmt, full tests with `PLANE_FUNCTIONAL_RUN=false` and local httptest access, the documented build/version workflow, `git diff --check`, credential-free functional help tests, and the `mise function-test` opt-in gate; all applicable checks passed and only functional tests/docs changed.*
