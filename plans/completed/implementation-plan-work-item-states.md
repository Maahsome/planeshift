# Implementation Plan: Work Item States

- [x] **🎟️Work Item States**
  - **Description:** Implement Jira ticket PSFT-14 / Work 05, the complete project-scoped Plane Work Item States resource. The implementation must cover exactly the five routes authorized by `@PUBLIC_API.txt`, reuse the shared `@plane` client/configuration/output seams, expose the resource through Cobra, preserve nullable and dynamic JSON values, and provide deterministic local coverage without Plane credentials. The official operation pages define the request fields, list controls, OAuth scopes, response/status behavior, and documented errors; the checked-in route inventory remains authoritative for inclusion.
  - **Scope:**
    - **In scope:** A new `@states` route/model package and deterministic tests; a top-level singular `state` command with plural `states` alias under `@cmd/state`; lazy registration in `@cmd/root.go`; command constants/tests in `@config/aliases.go`; safe help in `@help/state_help.go` and root discoverability in `@help/root_help.go`; focused state schema/response corrections in `@spec/plane-api.yaml`; and narrowly scoped command/model/test support needed to exercise all five operations.
    - **Out of scope:** State types, workflows, project features, project labels, work-item labels, cycles, modules, any other Plane resource or route, deprecated `/issues/` aliases, a second HTTP/configuration/authentication/output path, retries or asynchronous processing, generated/OpenAPI SDK code, new third-party dependencies, live Plane calls, credential changes, or unrelated changes to existing Project/Work Item behavior.
  - **Implementation Tasks:**
    - [x] **Govern:** Before choosing the resource package and CLI boundary, apply the **Owned Domains**, **Forbidden Dependencies & Protected Zones**, **Security & Resilience Hardlines**, **Architectural & Async Invariants**, and **Coding Conventions** sections of `@CONSTITUTION.md`; use ADR-002 in `@ARCHITECTURE.md` for the singular resource command with plural alias, registration below `cmd.RootCmd`, lazy `plane.ClientFactory`, and separation between `cmd/state`, `states`, `config`, `objects`, `help`, and `plane`. Keep all processing synchronous and preserve the existing dependency direction. If implementation would require a new transport, dependency direction, authentication model, or package boundary outside those decisions, stop and record it as an uncharted area requiring team review.
      - *Applied the five governing Constitution sections and ADR-002; the state resource uses the existing synchronous client, package direction, lazy factory, and Cobra hierarchy with no uncharted architectural area.*
    - [x] **Audit context and route contract:** Re-read `@AGENTS.md`, `@CONSTITUTION.md`, `@ARCHITECTURE.md`, `@LOCAL_BUILD.md`, `@work/README.md`, `@work/00-api-client-foundation.md`, `@work/01-projects.md`, `@work/05-work-item-states.md`, `@PLANE_CLIENT.md`, `@API_TESTING.md`, the existing `@plane/client.go`, `@plane/types.go`, `@plane/url.go`, `@projects/client.go`, `@projects/types.go`, `@cmd/project`, `@cmd/workitem`, `@objects/raw_json.go`, `@config/config.go`, and `@internal/testsupport/http.go` before editing. Reconfirm every linked official page: `@https://developers.plane.so/api-reference/state/overview.md`, `@https://developers.plane.so/api-reference/state/add-state.md`, `@https://developers.plane.so/api-reference/state/list-states.md`, `@https://developers.plane.so/api-reference/state/get-state-detail.md`, `@https://developers.plane.so/api-reference/state/update-state-detail.md`, and `@https://developers.plane.so/api-reference/state/delete-state.md`. Record the exact matrix next to the client so documentation naming cannot change the wire contract:
      - *Re-read the governance, client, resource, command, output, and test-support context; verified all five `PUBLIC_API.txt` routes against the six official state pages and recorded their wire-level contract below.*
      - **List:** `GET /api/v1/workspaces/{slug}/projects/{project_id}/states/`; workspace slug and project UUID; query `cursor`, `per_page` (default 20, maximum 100), `fields`, and `expand`; OAuth scope `projects.states:read`; 200 cursor page with grouping/count metadata and state results.
      - **Create:** `POST /api/v1/workspaces/{slug}/projects/{project_id}/states/`; required `name` and `color`, optional `description`, `sequence`, `group`, `is_triage`, `default`, `external_source`, and `external_id`; OAuth scope `projects.states:write`; current operation documentation reports a 200 JSON state response.
      - **Get:** `GET /api/v1/workspaces/{slug}/projects/{project_id}/states/{state_id}/`; no documented query parameters; OAuth scope `projects.states:read`; 200 JSON state response.
      - **Update:** `PATCH /api/v1/workspaces/{slug}/projects/{project_id}/states/{state_id}/`; all create properties are optional PATCH fields; OAuth scope `projects.states:write`; 200 JSON state response. Preserve omitted fields versus explicit empty, false, zero, and JSON null values.
      - **Delete:** `DELETE /api/v1/workspaces/{slug}/projects/{project_id}/states/{state_id}/`; OAuth scope `projects.states:write`; 204 with no response body, including the documented restriction that default or referenced states cannot be deleted.
      - **Response contract:** Model `id`, timestamps, `name`, nullable `description`, `color`, generated `workspace_slug`, `sequence`, `group`, `default`, optional `is_triage`/external fields, creator/updater IDs, `project`, and `workspace`; preserve future keys and explicit nulls. Reconcile the overview's string description of `sequence` with operation examples/body schemas that use a number by choosing a lossless JSON representation rather than coercing or dropping values. Treat the operation-page status/scope details as the implementation contract while retaining shared 2xx/error handling.
    - [x] **Reconcile the checked-in API contract:** Update only the Work Item States paths and schemas in `@spec/plane-api.yaml`. Keep the five `@PUBLIC_API.txt` routes, exact verbs, route segment roles, and trailing slashes; align the state path parameter token with the route inventory while documenting its workspace-slug meaning. Keep list-only `cursor`, `per_page`, `fields`, and `expand` parameters and remove those inherited controls from detail GET. Replace generic `JsonValue`, `JsonObject`, and `PaginatedJson` references for this slice with focused `State`, `StatePage`, `StateCreate`, and `StateUpdate` schemas; make create require `name`/`color`, make update properties optional, represent `sequence` as a lossless number/string/null-compatible value where the docs disagree, preserve additional response fields, and document 200 create/list/get/update plus 204 delete and default error responses. Do not alter neighboring resource paths, global auth schemes, or unrelated permissive schemas.
      - *Updated only the two state paths and four focused schemas; removed detail GET query controls, preserved exact inventory routes, and restored all neighboring contract hunks unchanged.*
    - [x] **Define typed state contracts:** Add `@states/types.go` with a `State` response model, presence/nullable-aware scalar fields, `json.RawMessage`-compatible handling for `sequence` and any other dynamic response value, and copied `Unknown map[string]json.RawMessage` data through custom marshal/unmarshal logic. Add `StatePage` based on `@plane.CursorPage[State]`, retaining cursor fields, `extra_stats` including explicit null, `grouped_by`, `sub_grouped_by`, `total_count`, and unknown page keys. Add `CreateStateRequest` with only the documented required/optional create properties and `UpdateStateRequest` with pointer/raw-message fields for every optional PATCH property; ensure `omitempty` omits nil but retains explicit empty strings, false, zero, and null. Validate only documented required create fields (`name` and `color`) and avoid speculative enum/range/project-ID validation that would reject future server values.
      - *Added typed state/page/request models with unknown-field retention, explicit-null and omission semantics, lossless sequence validation, and deterministic round-trip/presence tests in `states/types_test.go`.*
    - [x] **Implement the route client:** Add `@states/client.go` as a thin wrapper around `plane.Client`, following `@projects/client.go` and the established resource boundary. Build explicit collection/detail path helpers using `plane.EscapePathSegment` for workspace slug, project ID, and state ID, preserving every trailing slash and the inventory's `/workspaces/{slug}/projects/...` route family. Expose `Create`, `List`, `Get`, `Update`, and `Delete` methods with typed destinations and `plane.Response` metadata; send typed JSON bodies only for create/update, nil bodies for get/delete, list-only `url.Values`, and no resource-local auth, pagination transport, or error parsing. Document each method's official OAuth scope in comments without attempting to add a scope header.
      - *Added thin `states.Client` route wrappers for exactly the five inventory methods, with escaped path segments, trailing slashes, typed bodies/destinations, shared metadata, and documented OAuth-scope comments.*
    - [x] **Build list options and pagination:** Add a state `ListOptions` and `Query` method in `@states/types.go` (or a narrowly focused companion) that delegates to `plane.WithPagination` for `cursor`, `per_page`, `fields`, and `expand`, validates the shared 1–100 page-size range, and does not invent `order_by` or detail query controls. Preserve query values exactly, including cursors and comma-separated field/expand values.
      - *Delegated state collection queries to `plane.WithPagination`; tests verify exact cursor, page-size, fields, and expand encoding plus rejection outside 1–100 and absence of unsupported controls.*
    - [x] **Add CLI hierarchy and registration:** Add `StateCommandName = "state"` and `StateCommandAlias = "states"` in `@config/aliases.go`, with focused assertions in `@config/aliases_test.go`. Create `@cmd/state/state.go` plus one operation file each for `create.go`, `list.go`, `get.go`, `update.go`, and `delete.go`; register the parent from `@cmd/root.go` with the same lazy factory used by Project and Work Item. Expose `list` and `create` as `workspace_slug project_id`, and `get`, `update`, and `delete` as `workspace_slug project_id state_id`, using exact Cobra positional validation and no network/configuration access while the command tree is constructed.
      - *Added `state` with `states` alias, exact five Cobra operations/argument counts, lazy root registration, and focused alias/root hierarchy assertions.*
    - [x] **Wire request flags and output:** Add `@cmd/state/helpers.go` for changed-aware string, bool, and JSON/dynamic-sequence flag extraction. Create must require `--name` and `--color`; expose documented optional `--description`, `--sequence`, `--group`, `--is-triage`, `--default`, `--external-source`, and `--external-id`; update exposes the same fields as optional; list exposes only `--cursor`, `--per-page`, `--fields`, and `--expand`. Preserve explicit zero/false/empty/null request values and serialize an empty update as `{}` without adding an undocumented at-least-one-field rule. Validate list page size before resolving the client factory. Route successful JSON responses through `objects.NewRawJSONFromValue` and `Config.OutputData` so JSON, YAML, GRON, text/table fallback, and raw output remain centralized; keep delete's 204 success quiet.
      - *Added changed-aware request helpers and all documented flags; tests verify required create fields, exact explicit-value serialization, pre-factory list validation, centralized raw output, and quiet 204 delete.*
    - [x] **Add safe help:** Create `@help/state_help.go` and update `@help/root_help.go` only enough to make the new resource discoverable. Document the five operations, workspace/project/state argument order, list pagination/filter controls, required create fields, update partial semantics, supported state groups including the operation-page `triage` value, dynamic sequence input, 204 delete behavior, and existing output formats. Use placeholder identifiers only; do not include API keys, bearer tokens, invitation secrets, presigned URLs, upload fields, or other sensitive material.
      - *Added resource help and root discoverability for all five operations, argument ordering, supported controls/groups, dynamic sequence, partial updates, centralized formats, and quiet 204 deletes; safety assertions reject secret-bearing help text.*
    - [x] **Test the typed models:** Add `@states/types_test.go` covering the official state object and page examples, every documented wire name, pointer/null/omitted behavior, numeric and string sequence preservation, unknown scalar/object/array/null fields, grouping/count/cursor metadata, and JSON round trips. Assert exact create/update request serialization, required name/color validation, nil omission, explicit empty strings, false, zero, and raw null preservation; reject malformed documented scalar types rather than weakening the contract to make tests pass.
      - *Added and passed model tests for all documented wire fields, null/omitted and explicit-value semantics, numeric/string/null sequence preservation, unknown JSON values, page metadata, request validation, and malformed scalar rejection.*
    - [x] **Test all client operations:** Add table-driven `httptest` coverage in `@states/client_test.go` using `@internal/testsupport/http.go` for all five methods. Assert exact HTTP method, escaped path and trailing slash, list-only query values, absence of detail queries, selected API-key or bearer authentication with the other auth header absent, `Accept`, body-only `Content-Type`, JSON request bodies, typed response decoding, 200 response handling, 204 no-content behavior, pagination/grouping/unknown fields, and safe rate-limit metadata. Include representative shared typed API errors, bounded non-JSON errors, malformed-success JSON, empty success, validation-before-request, and credential-redaction cases without live credentials.
      - *Added and passed table-driven local-server coverage for create/list/get/update/delete, including escaped routes, auth/header isolation, bodies, status behavior, pagination metadata, errors, malformed/empty responses, validation, and redaction.*
    - [x] **Test CLI registration and behavior:** Add `@cmd/state/state_test.go`, extend `@cmd/root_test.go` and `@config/aliases_test.go` only as needed, and verify canonical/plural alias resolution, root parentage, exact operation arguments, documented flags and absence of unsupported flags, lazy factory invocation, list query construction, create/update body presence semantics, configured output dispatch, safe help, and quiet delete behavior. Use a fake injected `plane.Client` or local `httptest`; never require Plane credentials or network access.
      - *Added and passed command/config/root tests for alias and parentage, exact arguments and flags, lazy factory use, list queries, presence-aware create/update bodies, centralized output, safe help, and quiet delete.*
    - [x] **Audit shared boundaries and security:** Reuse `@plane/client.go`, `@plane/types.go`, `@plane/url.go`, `@internal/testsupport/http.go`, `@objects/raw_json.go`, and `@config/config.go` as-is where possible. Extend shared support only for a genuinely reusable assertion or model helper; do not add state-specific behavior to transport/config/output packages. Confirm API keys, bearer tokens, URLs containing secrets, and sensitive response values cannot enter logs, errors, help, or accidental default output; keep `go.mod`/`go.sum` unchanged.
      - *Verified the state slice uses only existing shared transport, URL, auth/error, pagination, testsupport, raw-output, and config seams; no shared code or dependency files changed, secrets are redacted/absent from help and output paths, YAML is valid, and `git diff --check` is clean.*
    - [x] **Verify acceptance criteria:** Confirm every one of the five matrix methods has exactly one typed client method, CLI command, request/response contract, and deterministic test; verify route escaping, trailing slashes, list pagination/filter controls, no detail GET query controls, documented OAuth-scope comments, nullable/dynamic/unknown JSON preservation, PATCH presence semantics, structured error handling, 200/204 behavior, and the absence of state-type/workflow/unrelated routes. Run `gofmt` on changed Go files, `CI=true go test -count=1 ./...`, `git diff --check`, and the non-interactive build/version workflow documented in `@LOCAL_BUILD.md`; validate the edited `@spec/plane-api.yaml` and confirm the documented build workflow remains valid.
      - *Verified one client/command/contract/test path for each of the five routes; confirmed exact escaped paths, trailing slashes, query/status/auth/presence/error behavior, no extra state routes, clean gofmt/diff, valid YAML, full tests, and the documented build/version JSON output.*
