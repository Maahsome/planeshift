# Work Ticket

ID: PSFT-17-20260917-1730-9D5B1D
Title: work item labels

## Description

### Work 06: Implement Plane API — Work Item Labels

Use this file as the implementation prompt for the public project-scoped label slice of `planeshift` (Jira ticket PSFT-17).

#### Prompt

You are an AI coding agent working in the `planeshift` repository. Read `AGENTS.md`, `CONSTITUTION.md`, `ARCHITECTURE.md`, `LOCAL_BUILD.md`, `work/README.md`, [`00-api-client-foundation.md`](./00-api-client-foundation.md), and [`01-projects.md`](./01-projects.md) before editing.

Implement the public project-scoped label catalog and CRUD operations completely. The source-controlled [`PUBLIC_API.txt`](../PUBLIC_API.txt) is authoritative for route inclusion. This prompt owns exactly 5 methods under the project `/labels/` route family. It does not own, preserve, or recreate the excluded workspace-level `/project-labels/` route.

##### Documentation to implement

- [Work Item Labels overview](https://developers.plane.so/api-reference/label/overview.md)
- [Create a label](https://developers.plane.so/api-reference/label/add-label.md)
- [List all labels](https://developers.plane.so/api-reference/label/list-labels.md)
- [Retrieve a label](https://developers.plane.so/api-reference/label/get-label-detail.md)
- [Update a label](https://developers.plane.so/api-reference/label/update-label-detail.md)
- [Delete a label](https://developers.plane.so/api-reference/label/delete-label.md)

##### Public route matrix — 5 methods

| # | Method | Exact path | Path parameters / role |
|---:|---|---|---|
| 1 | GET | `/api/v1/workspaces/{slug}/projects/{project_id}/labels/` | `{slug}` workspace slug, `{project_id}` project UUID; project label collection |
| 2 | POST | `/api/v1/workspaces/{slug}/projects/{project_id}/labels/` | `{slug}` workspace slug, `{project_id}` project UUID; create project label |
| 3 | GET | `/api/v1/workspaces/{slug}/projects/{project_id}/labels/{label_id}/` | `{slug}` workspace slug, `{project_id}` project UUID, `{label_id}` label UUID; detail |
| 4 | PATCH | `/api/v1/workspaces/{slug}/projects/{project_id}/labels/{label_id}/` | `{slug}` workspace slug, `{project_id}` project UUID, `{label_id}` label UUID; update |
| 5 | DELETE | `/api/v1/workspaces/{slug}/projects/{project_id}/labels/{label_id}/` | `{slug}` workspace slug, `{project_id}` project UUID, `{label_id}` label UUID; delete |

##### Dependencies and boundaries

- Reuse the shared client/configuration/output contract from [`00-api-client-foundation.md`](./00-api-client-foundation.md) and the project boundary from [`01-projects.md`](./01-projects.md).
- Keep this catalog distinct from any workspace-level label concept and from labels in removed initiative, release, or customer prompt files. The excluded `/project-labels/` route is not part of the public inventory.
- Do not add routes outside this matrix, generated SDK code, or third-party dependencies.

##### Required implementation behavior

- Read the linked operation pages and implement their exact request body, query parameters, OAuth scope, success status, response shape, and documented errors without changing the route matrix.
- Add typed request/response models with presence-aware PATCH fields and lossless handling for nullable or dynamic JSON values.
- Expose each method through the established Cobra/config/output conventions and expose documented pagination/filter controls for the collection.
- Keep API keys and sensitive response fields out of logs and accidental default output. Return bounded structured errors for JSON, empty, malformed, and non-JSON responses.
- Add deterministic `httptest` coverage for all 5 methods, including method/path/query/auth/body/decoding/status behavior and representative errors. Do not require live Plane credentials.

##### Definition of done

- Every method in the matrix has one client method, CLI surface, typed contract, and deterministic test.
- Pagination, nullable fields, dynamic JSON, and 204 responses are verified where applicable.
- `gofmt` is clean and `CI=true go test -count=1 ./...` passes.
- The documented build workflow in `LOCAL_BUILD.md` remains valid.
- Changes stay within this resource slice and focused shared test/model support.
